spring:
  profiles:
    active: local
    include:
      - member-postgres
      - order-postgres
      - payment-postgres
      - store-mongo
      - store-redis
logging:
  level:
    org.hibernate.SQL: trace
    org.hibernate.type.descriptor.sql.BasicBinder: trace
    org.fastcampus: debug
